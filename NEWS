dbox v0.0.3 (Release date: 2019-07-02)
=============

Changes:
* Omit missings at beginning of function; use completed cases only for all
  other processes.
* <normal> option plots an *idealized* normal distribution instead of random
  normal. Better for small samples.
* Added option to specify variable name via <label> argument.
* Better parsing of variable name via sys.call() when <label> is not specififed.
* Include linetype in legend.
* <fill> option affects boxplot fill as well as density plot fill.


dbox v0.0.2 (Release date: 2019-06-29)
=============

Changes:
* Renamed package and main function to dbox.
* Switched to ggplot native stat_boxplot() rather than several geoms for
  boxplot, with a significant speed improvement.
* Switched to geom_line() rather than geom_stat() for density plots to get
  orientatin the same as stat_boxplot().
* Now compute densities with n=512, with speed improvements.
